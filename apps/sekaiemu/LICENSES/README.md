# Licenses

This directory documents licenses for vendored or directly redistributed source artifacts
used by `Sekaiemu`.

Current entries:

- `libretro-api.txt`
  - license notice for the vendored `libretro.h` API header
