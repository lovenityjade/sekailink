# Debug Notes - 2026-06-21

## Frozen: LADX / RetroArch UDP bridge / wrapper receive path

Status: frozen on request. Do not keep drilling into this before the runtime
stack is realigned.

### What was fixed

- Link's Awakening DX no longer floods the AP server with stale checks after
  relaunch.
- Sekaiemu saves are now isolated per ROM/patch path, avoiding reused `.sav`
  files from previous generated rooms.
- The LADX beta client can connect through the RetroArch UDP bridge and reaches
  `Game connection ready`.
- The AP server does deliver `ReceivedItems` to the LADX wrapper.
- The LADX start-item gate was adjusted for the current patched ROM:
  `Tarin's Gift` / check `0x2A3` now expects bit `0x10` instead of the older
  `0x20`.

### Remaining bug

- Sending `Boomerang` to the LADX slot still does not visibly inject the item
  in-game.
- The likely unresolved question is whether the local runtime client/patch
  files are fully aligned with the server-side APWorld/generator update made on
  2026-06-20.
- Need verify that Client Core, Sekaiemu runtime, bundled AP client files,
  generated patch format, and Worlds/server APWorld are all using the same
  LADX beta version.

### Evidence from the last run

- Runtime client process connected to AP.
- RetroArch UDP bridge read memory successfully from Sekaiemu.
- `ReceivedItems` count increased after an item was sent.
- No confirmed memory write for the received Boomerang was observed before
  freezing the investigation.

### Next repair steps

1. Confirm exact server-side APWorld version for `Links Awakening DX Beta` on
   Worlds/generator.
2. Confirm local bundled runtime copy under `runtime/ap/worlds/ladx_beta`
   matches the server-side APWorld used to generate the `.apladxb` patch.
3. Confirm the generated `.apladxb` patch metadata/version matches the local
   wrapper client.
4. Relaunch with a fresh room and fresh isolated save.
5. Watch for:
   - `ReceivedItems`
   - `LADX receive state`
   - `LADX injecting item`
   - `retroarch_bridge_write`
6. If `ReceivedItems` appears but no `LADX receive state` appears, inspect the
   client guard before injection.
7. If `LADX injecting item` appears but no `retroarch_bridge_write` appears,
   inspect the RetroArch UDP `WRITE_CORE_MEMORY` bridge path.
8. If writes happen but the game ignores them, inspect LADX receive memory
   addresses and command/state bytes against the current APWorld patch.

### Do not forget

- Keep this compatibility fix at the adapter/client layer. Do not distort the
  generic BizHawk bridge to solve LADX-specific RetroArch behavior.
- Before more LADX testing, sync local runtime APWorld/client files from the
  same source as the generator.
- Keep Client Core and bootloader frozen except for critical runtime launch
  fixes.
