# ALTTP Default YAML

Ce dossier contient le YAML canonique par defaut attendu par le linkedworld
`ALTTP`.

Ce YAML appartient au module jeu.

But:

- materialiser `config/default.yaml` a un chemin stable
- expliciter la presentation side-by-side et les attentes metadata
- pointer vers l'inventaire run-complete `tracker/default.bundle/surface.complete.json`
- rester honnete sur le fait que cette baseline est repo-owned et minimale
- fournir a `Sekaiemu` une vue packaging/runtime du contenu ALTTP

Ce YAML:

- complete le contrat clean-room
- ne revendique pas une equivalence stricte avec un YAML tiers historique
- sert de base de packaging et de documentation test
