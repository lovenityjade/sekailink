# Evolution Room Query Service Deployment

This directory contains the first deployment artifacts for the native `evolution` room-query service.

Files:
- `evolution_room_query.json.example`
- `sekailink-evolution-room-query.service`

This service is designed to stay behind Apache/reverse proxy and read room projections from SQLite or MySQL.
