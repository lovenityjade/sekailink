# Source Of Truth

For `sekailink-evolution`, authoritative code and config are:

1. `server/native/sekailink_server_core` for native server runtime logic.
2. `deploy/evolution` for Evolution service deployment units and templates.
3. `third_party/sekailink_runtime/third_party` for embedded build headers.

Planning/history documents in `docs` are reference context only.
