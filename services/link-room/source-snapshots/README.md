# Source Snapshots Warning

These directories are historical or candidate source snapshots. They are not
automatically the canonical source for any live service.

Before changing or deploying anything from here, check:

```text
docs/repo-cleanup/LIVE_SERVICE_MAP.md
```

Do not use this tree as a shortcut for `sekailink-chat-api` work unless the live
service map proves that the active service is built from this source.
