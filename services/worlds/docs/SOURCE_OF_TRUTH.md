# Source Of Truth

For `sekailink-worlds`, authoritative code and config are:

1. `server/native/sekailink_server_core` for native generation/runtime logic.
2. `deploy/worlds` for Worlds service deployment units and templates.
3. `third_party/sekailink_runtime/third_party` for embedded build headers.

Planning/history documents in `docs` are reference context only.
